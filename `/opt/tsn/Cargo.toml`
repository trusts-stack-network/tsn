[workspace]
members = [
    ".",
    "wallet",
    # "fuzz",        # désactivé temporairement – le dossier / fuzz_targets n’existe pas encore
]

# … reste du fichier inchangé …

[[bin]]
name = "tsn"
path = "src/main.rs"

# Le binaire fuzz_runner est retiré jusqu’à ce que le fuzzing soit implémenté.
# [[bin]]
# name = "fuzz_runner"
# path = "fuzz/fuzz_targets/runner.rs"