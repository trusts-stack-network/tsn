// v2.1.2: ForkChoice was unused dead code (836 lines).
// Fork choice logic is implemented directly in core::blockchain (try_add_block,
// should_prefer_candidate, reorganize_to_block).
// This module is kept empty to avoid breaking the module tree.
