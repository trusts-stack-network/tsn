// Assurez-vous que tous les files Rust sont correctly writtens et compiled.
// Si l'erreur ne se trouve pas dans transaction.rs, il est possible qu'elle se trouve
// dans un autre file, comme pow.rs ou block.rs. Verify ces files pour
// vous assurer qu'il n'y a pas d'errors de syntaxe ou de delimiters non closeds.

// Exemple de verification dans pow.rs :
fn proof_of_work() {
    // Code de la preuve de travail
    // Assurez-vous que toutes les fonctions et les blocs de code soient correctly closeds.
}

// Exemple de verification dans block.rs :
fn block_validation() {
    // Code de validation du bloc
    // Verify que tous les fields du bloc soient correctly defineds et initializeds.
}