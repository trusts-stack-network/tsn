use proptest::prelude::*;
use src::crypto::{encrypt, decrypt};

proptest! {
    #[test]
    fn test_encryption_decryptionroundtrip(data: Vec<u8>) {
        let encrypted = encrypt(&data);
        let decrypted = decrypt(&encrypted);
        prop_assert_eq!(data, decrypted);
    }
}